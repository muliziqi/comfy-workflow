# 会话总结 — ComfyUI 制图流搭建全程

> 会话日期:2026-09-06 · 搭建者:ZCode(AI 编程助手)· 仓库:https://github.com/muliziqi/comfy-workflow

## 一、起点与问题诊断

用户需求:"帮我打通 comfy 制图流"。

排查发现:

- ComfyUI Desktop 安装于 `D:\Comfy-Desktop`(v0.28.0,Python 3.13.12,torch 2.12.1+cu130)
- 硬件:RTX 5060 Laptop 8GB 显存 / 16GB 内存
- **程序完好但模型全缺**:`checkpoints` 里只有 8 月 20 日下载中断留下的 0 字节 `.part` 残件,整条链路断在"没有可用模型"
- 服务未运行;机器无 gh CLI、无 GitHub 凭据、系统 Python 不在 PATH

## 二、搭建过程

### 1. 模型下载

- 选型:SDXL base 1.0(6.5GB,适配 8GB 显存);镜像走 hf-mirror.com(HuggingFace 国内镜像)
- 实测单线程约 3.4 MB/s,改用 **4 路并行分段下载**(自写 `download_sdxl.sh`,支持断点续传),约 8 分钟完成
- 文件字节数精确校验(6,938,078,334 与源一致),落位 `ComfyUI-Shared\models\checkpoints\`

### 2. 服务与脚本

- `extra_models_config.yaml`:把 `ComfyUI-Shared\models` 挂给无头启动的 ComfyUI
- `start_comfyui.bat`:一键启动服务(后升级为支持端口参数)
- `comfy_gen.py`:零依赖(纯标准库)API 出图脚本,提交 `/prompt` → 轮询 `/history` → `/view` 下载成图
- 服务运行于 `http://127.0.0.1:8188`,与桌面版共用模型目录

### 3. 端到端验证

首次生成即成功:宇航员橘猫(1024×768,28 步),画面与提示词完全吻合。

### 4. GitHub 仓库

- winget 安装 gh CLI v2.100.0 → 设备码流程登录(账号 **muliziqi**)→ 创建仓库并推送
- 仓库初期为私有,后应用户要求转**公开**(已核对仓库内无密钥/隐私数据)

### 5. 多风格系统(第二轮迭代)

`comfy_gen.py` 新增 `--style` 预设,9 种风格:`none / ink 水墨 / photo 写实 / anime 动漫 / oil 油画 / guofeng 国风 / cyberpunk 赛博朋克 / 3d / sketch 素描`。

每个预设三件事:正向叠加英文风格词、负向合并干扰排除项、套用建议尺寸。实测四种风格(水墨/摄影/动漫/油画)出图风格全部成立。

**沉淀的提示词经验**:风格词给英文(SDXL 响应更准)、内容词可中文、负面显式排除干扰风格、构图意图要显式写(close-up / wide shot)。

## 三、可靠性加固(第三轮迭代)

| 事项 | 处理与证据 |
|---|---|
| 服务生命周期 | 停掉会话内服务,用 .bat 重新启动,进程树确认脱离 ZCode 会话 —— 现服务即 .bat 形态,会话关闭不影响 |
| .bat 实测 | 实际启动验证通过(API 应答 + 进程链检查) |
| ComfyUI-Manager SSL | `bypass_ssl` 由 true 改回 **false**,恢复装节点时的证书校验 |
| cmd 中文乱码 | 脚本启动时主动切控制台 UTF-8(SetConsoleOutputCP 65001) |
| 端口共存 | .bat 支持端口参数;出图脚本新增 `--server`;README 写明与桌面版不抢 8188 |
| 显存上界(实测纠偏) | 1024² ≈26 秒/张、1536² ≈44 秒/张**均实测稳定**;此前"1536 大概率爆显存"的预判被推翻;>1536 档与 `--lowvram` 如实标注未实测 |
| README 准确性 | "一条命令出图"修正为"出图两步走";新增「可调整的细节」章节(提示词写法/采样参数/显存实测表/端口与模型切换) |

## 四、当前状态

- 服务:经 `start_comfyui.bat` 启动,8188 端口,独立窗口进程(桌面最小化窗口)
- 模型:SDXL base 1.0 唯一 checkpoint;新模型放入 `ComfyUI-Shared\models\` 对应子目录即用
- 仓库:公开,main 分支,3 个功能 commit,工作区干净
- 出图:`python.exe scripts\comfy_gen.py -p "内容" --style 风格`,成图存 `output\`(不入库)

## 五、遗留与可选后续

1. **内容漂移**:SDXL 基础模型对多物体/特写构图遵循弱;缓解手段已写入 README,根治需换更强 checkpoint(如需下载对照模型,脚本现成)
2. **未实测项**:2048² 以上尺寸、`--lowvram` 参数、.bat 的 8189 分支(逻辑与已验证分支一致)
3. **开机自启**:未配置,重启后需手动双击 .bat;如需自动化可用计划任务实现
4. **生成速度**:如需更快的出图,可考虑 SDXL Turbo/Lightning 类蒸馏模型或 LCM-LoRA

## 六、产出物清单

| 文件 | 作用 |
|---|---|
| `README.md` | 完整使用文档(AI 辅助说明 + 两步出图 + 风格表 + 可调整细节) |
| `scripts/start_comfyui.bat` | 启动服务(可选端口参数) |
| `scripts/extra_models_config.yaml` | 模型目录挂载配置 |
| `scripts/comfy_gen.py` | AI 辅助多风格出图脚本(纯标准库) |
| `scripts/download_sdxl.sh` | 模型四路并行下载(备用) |
| `output/` | 生成图片(本地,gitignore) |
